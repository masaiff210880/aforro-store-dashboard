import PageShell from '../components/common/PageShell';

export default function ReturnReplacement() {
    return (
        <PageShell
            title="Return & Replacement"
            description="Process returns and replacements with ease."
        />
    );
}
