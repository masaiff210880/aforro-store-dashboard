import PageShell from '../components/common/PageShell';

export default function OrderHistory() {
    return (
        <PageShell
            title="Order history"
            description="Browse completed and past orders in one place."
        />
    );
}
